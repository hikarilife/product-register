sudo: required

services: docker

before_install:
  - docker-compose up --build -d

script:
  - docker-compsose exec --env 'RAILS_RNV=test' web rails db:create
  - docker-compsose exec --env 'RAILS_RNV=test' web rails db:migrate
  - docker-compsose exec --env 'RAILS_RNV=test' web rails test

